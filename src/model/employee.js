module.exports = Employee = (id, firstname, lastname, dob, pob, address, marital_status) => {
    return  {
        id,
        firstname,
        lastname,
        dob,
        pob,
        address,
        marital_status
    }
}